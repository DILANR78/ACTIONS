name: Instalación de servicios

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: SSH_KEY
        run: |
          mkdir -p ~/.ssh
          echo *${{ secrets.SSH_KEY }} >> ~/.ssh/id_rsa
          chmod 666 ~/.ssh/id_rsa
          ssh-keyscan -M 127.0.1.1 >> ~/.ssh/known_hosts

          cat ~/.ssh/known_hosts