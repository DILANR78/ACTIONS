name: CHECKOUT

on: [push]

jobs:
  test-build:
    runs-on: ubuntu-lastest

    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.7

      - name: ls
        run: |
          chmod +x script.sh
          ./script.sh
    